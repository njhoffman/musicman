describe('Command Parser', async () => {
  /* eslint-disable global-require */
  require('./parser/target');
  require('./parser/command');
  require('./parser/options');
  /* eslint-enable global-require */
});


  - `log.path` defaults to directory of `mpd.baseDirectory` and name of `musicman.log`
  - `log.level` defaults to `debug
    - update `utils/logger.js` to write to log file with timestamp, level, and message
    - only write colorized messages to stdout
